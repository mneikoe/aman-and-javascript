// array methods
let arr = ['a','b','c','d',"e"]
// slice method we can extract part of an array without changing the original array
console.log(arr.slice(2))
console.log(arr.slice(2,4))
console.log(arr.slice(-2))
console.log(arr.slice(1,-2))

// done in copy